const initState = {
    title:'sugar baby'
};

export default (state = initState, action) => {
    switch (action.type) {
        case 1:
        default:{
            return state;
        }
    }
}
